{
  "name": "@figma-web-import/figma-plugin",
  "version": "0.0.1",
  "description": "Figma plugin to import captured web pages as editable designs",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@figma-web-import/shared": "workspace:*",
    "preact": "^10.24.3",
    "zustand": "^5.0.1"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.101.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
